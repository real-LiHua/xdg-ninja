{
    "name": "idapro",
    "files": [
        {
            "path": "$HOME/.idapro",
            "movable": true,
            "help": "```\nexport IDAUSR=\"$XDG_CONFIG_HOME\"/idapro\n```\n\n_After the configuration migration is complete_, create the file `$XDG_CONFIG_HOME/idapro/plugins/remove_dot_idapro.py`, and place the following code inside it so that IDA Pro removes the `~/.idapro` directory when it starts:\n\n```python #!/usr/bin/env python3\nimport os\nimport shutil\nimport stat\nfrom pathlib import Path\n\nimport ida_idaapi\n\n\nclass MyPlugin(ida_idaapi.plugin_t):\n    flags = ida_idaapi.PLUGIN_UNL | ida_idaapi.PLUGIN_MULTI | ida_idaapi.PLUGIN_FIX\n    wanted_name = \"Remove ~/.idapro\"\n\n    def init(self):\n        print(\">>>Remove ~/.idapro: called.\")\n        shutil.rmtree(Path.home() / \".idapro\", onexc=self.remove_readonly)\n        return ida_idaapi.plugmod_t()\n\n    def remove_readonly(self, func, path, _):\n        os.chmod(path, stat.S_IWRITE)\n        func(path)\n\n\ndef PLUGIN_ENTRY():\n    return MyPlugin()\n```\n"
        }
    ]
}
